@model Avocado.WEB.Models.ViewModels.HomeIndexVM
@{
	ViewData["Title"] = "Home Page";
}

<div class="text-center">
	<h1 class="display-4">Avocado</h1>
	<div class="row">
		<div class="col-md-2"></div>
		<div class="col-md-8">
			<ul class="py-3">
				@foreach (var item in Model.Categories)
				{
					<li class="list-group-horizontal btn btn-outline-dark">@item.Name</li>
					if (Model.Products.Where(x => x.CategoryId == item.Id) != null)
					{
						foreach (var items in Model.Products.Where(x => x.CategoryId == item.Id))
						{
							<div class="card  mb-3">
								<div class="card-header btn btn-outline-danger  border-danger">@items.Name</div>
								<div class="card-body text-success btn btn-outline-light">
									
										<img src="~/images/@items.ImgUri" class="img-fluid rounded-start" alt="...">
									
								</div>
								<div class="card-footer btn btn-outline-success border-success">
									<div>
										<span>$@items.Price</span>									
										<a asp-action="Details" asp-route-id="@items.Id" class="btn btn-success offset-8">Details</a>
									</div>
									
								</div>
							</div>
						}
					}
				}
			</ul>
		</div>
		<div class="col-md-2"></div>
	</div>

</div>
